--- a/crates/ferromark_parser/src/parser/source_normalization.rs
+++ b/crates/ferromark_parser/src/parser/source_normalization.rs
@@ -3,6 +3,10 @@
 use ferromark_ast::Span;
 
 use super::spans::SpanMap;
+
+// DIAGNOSTIC ONLY: skips required NUL/BOM normalization on a preverified corpus.
+pub(super) static SKIP_NORMALIZATION: std::sync::atomic::AtomicBool =
+    std::sync::atomic::AtomicBool::new(false);
 
 /// One generated byte offset per replacement, not one entry per source byte.
 /// Only the root parser owns this map; nested parsers already see normalized
@@ -18,6 +22,9 @@
     source: &'a str,
     body_start: usize,
 ) -> (&'a str, Option<&'a NormalizedSourceMap<'a>>) {
+    if SKIP_NORMALIZATION.load(std::sync::atomic::Ordering::Relaxed) {
+        return (source, None);
+    }
     let bytes = source.as_bytes();
     // Only a BOM at the original document start is an encoding marker.
     // A BOM after an extracted metadata block is ordinary body content.
--- a/crates/ferromark_parser/src/parser.rs
+++ b/crates/ferromark_parser/src/parser.rs
@@ -146,6 +146,10 @@
 
     /// Creates a new parser with the specified options.
     #[must_use]
+    pub fn set_diagnostic_skip_normalization(skip: bool) {
+        source_normalization::SKIP_NORMALIZATION.store(skip, std::sync::atomic::Ordering::Relaxed);
+    }
+
     pub fn with_options(allocator: &'a Allocator, source: &'a str, options: ParserOptions) -> Self {
         let front_matter = options.front_matter.then(|| front_matter::extract(source)).flatten();
         let body_start = front_matter.as_ref().map_or(0, |metadata| metadata.span.end as usize);
