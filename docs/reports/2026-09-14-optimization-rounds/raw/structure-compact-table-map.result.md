# Structure variant: compact table-cell source map

Result: candidate retained for parent review.

Reason: the implementation preserves table cell output and absolute source
spans while changing escaped-pipe remapping metadata from one `u32` per
generated byte to one generated position per removed backslash. Focused table
tests, table snapshots, full workspace tests, formatting, and clippy passed.

No timing claim is made in this round; the parent harness should measure long
cells with sparse escaped pipes, dense escaped pipes, and ordinary tables.
