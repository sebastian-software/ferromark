# Byte-exact inputs for the workflow-pin contract tests: several are
# deliberately malformed YAML, and reformatting any of them would change what
# the test asserts. oxfmt auto-discovers this file.
test-fixtures/
